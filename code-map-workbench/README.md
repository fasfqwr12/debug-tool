# Code Map Workbench

独立的代码图谱上位机工程。

当前目标：

- 导入 C/C++ 工程目录
- 左侧浏览文件与函数
- 右侧整文件阅读
- 在调用点内联展开被调函数
- 点击变量/宏/类型查看源头与定义片段

运行：

```powershell
py main.py
```

或双击项目根目录下的 `start.bat` 一键启动。

默认优先启动桌面窗口（`pywebview`）。

如果本机缺少桌面运行时，则退回浏览器打开 `http://127.0.0.1:8876/`。
